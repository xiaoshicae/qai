---
description: /commit 的简写别名
allowed-tools: Bash(git:*, cargo:*, npm:*, npx:*, echo*, CLAUDE_COMMIT=1 git commit:*), Read, Glob, Grep, AskUserQuestion
argument-hint: [commit-message]
---

# 简写提交命令

本命令为 `/commit` 的简写别名，请按 `/commit` 的流程执行。

## 用法示例

```
/c
/c @fix: 修复请求超时
```
